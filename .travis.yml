language: python
python:
  - '3.5'
install: 'pip install -r requirements.txt && pip install -r tests/requirements.txt'
script: 'nosetests --with-coverage --cover-package shotsfired && pylint shotsfired'
